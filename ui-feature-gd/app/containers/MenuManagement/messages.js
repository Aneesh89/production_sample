/*
 * MenuManagement Messages
 *
 * This contains all the text for the MenuManagement container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MenuManagement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MenuManagement container!',
  },
});
