// import { selectGeneralDiaryDomain } from '../selectors';

describe('selectGeneralDiaryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
