/*
 * GeneralDiary Messages
 *
 * This contains all the text for the GeneralDiary container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GeneralDiary';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GeneralDiary container!',
  },
});
